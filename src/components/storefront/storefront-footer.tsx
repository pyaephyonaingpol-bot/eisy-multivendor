import { SitePortalFooter } from "@/components/layout/site-portal-footer";

/** Buyer storefront footer — copyright strip only. */
export function StorefrontFooter() {
  return <SitePortalFooter />;
}
